import React from 'react';
import '../styles/HeadDisplay.css'; // Import the CSS file for styling

const Header = () => {
  return (
    <header className="app-header">
      <h1>UnderPass Real-Time Water Level Monitor</h1>
    </header>
  );
};

export default Header;